{
    "buildCommand": "bash tool/build.sh",
    "outputDirectory": "build/web",
    "ignoreCommand": "git diff --quiet HEAD^ HEAD .",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}